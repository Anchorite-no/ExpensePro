# ExpensePro 环境变量配置
# 使用方法：cp .env.example .env 然后填入实际值

# ======== Docker 部署 ========
# Docker 使用以下变量自动拼接 DATABASE_URL
DB_PASSWORD=your_secure_password
DB_NAME=expense_pro

# ======== 本地开发 ========
# 本地开发时需要手动写完整连接串，放到 server/.env 中：
# DATABASE_URL="mysql://root:your_password@localhost:3306/expense_pro"
# PORT=3001

# ======== 通用配置 ========
# JWT 密钥（建议：openssl rand -hex 32）
JWT_SECRET=change_me_to_a_secure_secret_key

# AI 地址
AI_BASE_URL=https://generativelanguage.googleapis.com/v1beta

# ======== 私有版功能开关（留空=不启用） ========
# 邀请码注册
INVITE_CODE=
# 服务端 AI 密钥（填入后用户无需自行配置 Key）
SERVER_AI_KEY=
# E2E 加密（设为 true 则管理员无法查看用户账单数据）
ENCRYPTION_ENABLED=

# ======== 代理（可选） ========
# HTTP_PROXY=http://host.docker.internal:7890
# HTTPS_PROXY=http://host.docker.internal:7890

NODE_ENV=production
