# Database Configuration
# 生成随机数据库密码命令: openssl rand -base64 16
DB_PASSWORD=your_secure_password
DB_NAME=expense_pro

# Application Secrets
# 生成随机JWT密钥命令: openssl rand -hex 64
JWT_SECRET=change_me_to_a_secure_secret_key

# AI Service Configuration (Gemini)
AI_BASE_URL=https://generativelanguage.googleapis.com/v1beta

# Network Proxy (Optional)
# 如果你的服务器需要代理才能访问 Google API，请配置以下项
# Docker 部署时使用 host.docker.internal 指向宿主机代理
# HTTP_PROXY=http://host.docker.internal:7890
# HTTPS_PROXY=http://host.docker.internal:7890

# Node Environment
NODE_ENV=production
